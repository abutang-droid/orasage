# Orasage 八字排盘计算器 — 项目文档

**品牌**: Orasage (Ora + Sage)  
**技术栈**: React 19 + Express 4 + tRPC 11 + Drizzle ORM + MySQL + Tailwind CSS 4  
**语言**: TypeScript 5.9  
**测试**: Vitest（6 文件 / 54 测试全部通过）

---

## 1. 项目概述

Orasage 是一个八字（四柱）命理排盘计算器，支持公历/农历输入，可进行单人排盘和双人合盘分析。核心计算引擎在浏览器端完成，后端提供用户认证、AI 解读、历史记录存储。

### 核心功能

| 功能 | 状态 |
|------|------|
| 单人八字排盘 | ✅ 完成 |
| 双人合盘分析 | ✅ 完成 |
| 真太阳时修正（均时差查表） | ✅ 完成 |
| 城市搜索（拼音/中文/英文，592城） | ✅ 完成 |
| 农历-公历互转 | ✅ 完成 |
| AI 解读报告（分节折叠） | ✅ 完成 |
| 五行手串推荐 | ✅ 完成 |
| 今日运势 | ✅ 完成 |
| 计费方案（3档） | ✅ 完成 |
| 历史记录 | ✅ 完成 |

---

## 2. 架构概览

```
bazi-calculator/
├── client/src/               # 前端（React 19）
│   ├── pages/
│   │   ├── Home.tsx          # 首页：表单输入 + 模式切换
│   │   └── HistoryPage.tsx   # 历史记录页
│   ├── components/
│   │   ├── BaziResult.tsx    # 结果展示（单人/双人/计费墙/AI解读）
│   │   ├── WheelPicker.tsx   # 日期选择器（DatePicker）
│   │   └── PlanSelectionModal.tsx  # 计费选择弹窗
│   ├── lib/
│   │   ├── bazi.ts           # 核心计算引擎（77KB）
│   │   ├── lunarData.ts      # 农历数据查询（1900-2100）
│   │   └── cityData.ts       # 城市坐标（内嵌200城）
│   └── App.tsx               # 路由配置
├── server/                    # 后端（Express 4）
│   ├── routers.ts            # tRPC 接口
│   ├── db.ts                 # 数据库操作
│   └── _core/                # 框架层（auth, env, llm, storage）
├── shared/
│   ├── types.ts              # 共享类型 + 计费方案定义
│   └── const.ts
├── drizzle/
│   └── schema.ts             # 数据库表结构
├── scripts/
│   └── generate-lunar-data.mjs  # 农历数据生成脚本
└── client/public/data/       # 本地数据文件（21个JSON）
```

---

## 3. 设计系统（OraSage DS v1.1）

全站 UI 遵循 **DS v1.1 单色规范**，详见仓库根目录：

- 规范文档：`docs/design-system/OraSage-Design-System-v1.1-Revised.md`
- Token 实现：`shared/design-tokens/orasage-tokens.css`
- 应用样式：`bazi/client/src/index.css`、`bazi/client/src/bazi-home.css`

| Token | 色值 | 用途 |
|------|------|------|
| Pure Black | `#171717` | 主按钮、标题、一级文字 |
| Deep Gray | `#6B7280` | 二级文字、占位符 |
| Light Gray | `#E7E5E4` | 边框、分割线 |
| Soft Surface | `#FAFAF8` | 页面背景 |
| Pure White | `#FFFFFF` | 卡片、输入框背景 |

- **标题字体**: Noto Serif SC / Source Han Serif SC（衬线）
- **正文字体**: Noto Sans SC / Inter（无衬线）
- **圆角**: 12px（按钮）、卡片见 DS v1.1
- **四柱结果区**：可保留深色对比块（内容语义区，非品牌色板）

---

## 4. 数据流

### 排盘流程

```
用户输入 → Home.tsx (handleSubmit)
  → toInput() 转换数据 + getCityCoords() 查询城市坐标
  → calcSingleBazi() / calcDoubleBazi()
    → getLunarData() 查农历干支
    → calcWuXingWeighted() 五行藏干加权
    → analyzeStrength() 身强弱分析
    → determineFavorable() 喜用神/忌神
    → calcDaYunNew() 大运排列
    → calcShensha() 神煞
  → SingleBaziResultView 展示结果
```

### 支付流程

```
结果页计费墙 → 选择方案（basic/advanced/premium）
  → handlePlanSelect() 解锁内容
  → 展示完整命盘 + AI解读 + 手串推荐
```

---

## 5. 计费方案

| 方案 | 价格 | 内容 |
|------|------|------|
| 基础方案 (basic) | ¥9.9 | 完整命盘分析 |
| 深度解读 (advanced) | ¥99 | 命盘分析 + 流年大运 + 五行手串
| 终身会员 (premium) | ¥299 | 命盘分析 + 流年大运 + 五行手串 + 专属大礼盒

定义在 `shared/types.ts` → `PLAN_OPTIONS`

---

## 6. 快速开始

```bash
# 安装
pnpm install

# 生成农历数据（首次运行）
node scripts/generate-lunar-data.mjs

# 配置环境变量 (.env)
# DATABASE_URL=mysql://root@localhost:3306/bazi_calculator
# JWT_SECRET=随机字符串
# VITE_APP_ID=bazi-calculator
# VITE_OAUTH_PORTAL_URL=http://localhost:3000
# VITE_LUNAR_DATA_DIR=/data

# 启动
pnpm db:push
pnpm dev
```

---

## 7. 常用命令

```bash
pnpm dev          # 启动开发服务器
pnpm build        # 构建生产版本
pnpm test         # 运行测试
pnpm check        # TypeScript 类型检查
pnpm format       # 代码格式化
pnpm db:push      # 同步数据库表结构
```

---

## 8. 测试覆盖

| 文件 | 测试数 |
|------|--------|
| server/bazi.city.test.ts | 16 |
| server/bazi.pinyin.test.ts | 15 |
| server/bazi.records.test.ts | 7 |
| server/bazi.truesolar.test.ts | 8 |
| server/bazi.sections.test.ts | 7 |
| server/auth.logout.test.ts | 1 |
| **总计** | **54** |

---

## 9. 关键约定

- **tRPC-first**: 后端调用统一使用 `trpc.*.useQuery/useMutation`
- **Auth**: 通过 `isAuthenticated` 判断登录状态
- **Datetime**: DB 存 UTC Unix 时间戳，展示时转本地时区
- **数据类型**: PlanType = `"basic" | "advanced" | "premium"`
- **农历数据**: 本地 `/client/public/data/` 存储 JSON，按十年分文件，文件名有哈希后缀
- **城市数据**: 内嵌在 `cityData.ts` 中（~200 城），无需外部加载

---

## 10. 部署配置

### Docker 容器化部署

项目通过 Docker Compose 部署，配置文件：
```
/Users/je/WorkBuddy/2026-06-15-10-35-02/server-setup/docker-compose.yml
```

一键更新脚本：
```bash
bash /Users/je/WorkBuddy/2026-06-15-10-35-02/update-bazi.sh
```

### 访问地址

| 方式 | 地址 |
|------|------|
| 本地 | http://localhost:3000 |
| 外网 | https://api1.lilyfunnlove.com |

### 环境变量

| 变量 | 用途 |
|------|------|
| `DATABASE_URL` | MySQL 连接串 |
| `WORDPRESS_URL` | 关联的 WordPress 站点（https://www.c2.pub） |
| `WP_WOO_KEY` / `WP_WOO_SECRET` | WooCommerce REST API 密钥 |
| `DEEPSEEK_API_KEY` | AI 解读 API 密钥 |

### 报告文件持久化

用户购买报告后，系统生成静态 HTML 报告，保存路径：

| | 路径 |
|------|------|
| 容器内 | `/app/dist/public/reports/` |
| 宿主机 | `./server-setup/bazi-reports/` |

> 宿主机目录通过 Docker volume 挂载，容器重建后报告不会丢失。
