/* 该文件通常由 `npm run generate:importmap` 自动生成。
   当前配置未使用自定义 admin 组件，故导出空映射作为占位；
   首次 `npm install` 后建议运行一次 `npm run generate:importmap` 重新生成。 */

/** @type import('payload').ImportMap */
export const importMap = {};
