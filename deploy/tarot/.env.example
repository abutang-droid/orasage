# tarot.orasage.com — proxy 模式环境变量（历史遗留回滚选项）
# native 自托管模式请参考 ../../tarot/.env.example
# 复制为 .env 后修改

# 塔罗从未真正上线过反代版本，此变量从未在任何文档中被确认过真实值，
# 使用该模式前必须先自行确认。
TAROT_UPSTREAM_URL=

# Docker 镜像（proxy 模式默认本地构建）
TAROT_IMAGE=orasage-tarot-proxy:local
